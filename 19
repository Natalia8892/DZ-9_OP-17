<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Задание 19</title>
    <style>
        #myunique {
            transition: all 2s;
        }
    </style>
</head>

<body>
    <p id="myunique" onclick="Format()">TEXT</p>
    <script>
        function Format() {
            var fontSize = parseInt(window.getComputedStyle(myunique, null).getPropertyValue('font-size')) * 2 + "px";
            myunique.style.fontSize = fontSize;

            var width1 = parseInt(window.getComputedStyle(myunique, null).getPropertyValue('width')) * 2 + "px";
            myunique.style.width1 = width1

            var height1 = parseInt(window.getComputedStyle(myunique, null).getPropertyValue('height')) * 2 + "px";
            myunique.style.height1 = height1;
        };
    </script>
</body>

</html>
